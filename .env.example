# Use .dev.vars for the local Wrangler runtime (see .dev.vars.example).
# Use these same names in environments that provide process environment variables.
AI_API_KEY=
AI_BASE_URL=https://your-provider.example/v1
AI_MODEL=your-model-name
